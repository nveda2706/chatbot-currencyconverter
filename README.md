# Currency Converter Chatbot

A simple chatbot built with **Flask** that converts currencies using the [ExchangeRate API](https://www.exchangerate-api.com/).  
This bot is designed to run on **Telegram**.
## Features
- Convert between different currencies in real time.
- Uses ExchangeRate API for accurate conversion rates.
- Telegram bot integration.
- Secure API key management with `.env`.
